# HTTP server
PORT=3000

# Worker: cron schedule (default: every 5 minutes)
# Pattern: sec min hour day month weekday
COMPARE_CRON=0 */5 * * * *

# Comma-separated list of feed URLs to compare
FEED_URLS=https://hnrss.org/frontpage,https://lobste.rs/rss
